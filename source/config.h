#pragma once
/* ============================================================
   config.h — config load/save
   ============================================================ */
#include "types.h"

void loadConfig(void);
void saveConfig(void);
void saveDefaultConfig(void);

